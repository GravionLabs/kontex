{
  "name": "@gravionlabs/kontex-types",
  "version": "1.0.1",
  "description": "Shared types, state management, and event bus for kontex projects",
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepublish": "pnpm build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run --coverage",
    "check": "biome check --write src/"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GravionLabs/kontex.git"
  },
  "files": [
    "dist",
    "src"
  ],
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.8",
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
